import React from 'react';
import { Table } from 'antd';

function Menu() {
  return (
    <div className='menu'>
      <Table bordered dataSource={[]} columns={[]} />
    </div>
  );
}

export default Menu;
